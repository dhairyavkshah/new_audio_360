{
  "name": "new-audio-360",
  "version": "1.0.0",
  "description": "Native Android music player app - build via GitHub Actions",
  "private": true,
  "scripts": {
    "lint": "prettier --check \"**/*.{kt,xml,json,md}\"",
    "format": "prettier --write \"**/*.{kt,xml,json,md}\""
  },
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
